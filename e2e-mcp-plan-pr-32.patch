diff --git a/cypress/e2e/tests/features/mcp-test-plan-keyboard-shortcuts.md b/cypress/e2e/tests/features/mcp-test-plan-keyboard-shortcuts.md
index 2f44359..d0f3622 100644
--- a/cypress/e2e/tests/features/mcp-test-plan-keyboard-shortcuts.md
+++ b/cypress/e2e/tests/features/mcp-test-plan-keyboard-shortcuts.md
@@ -30,8 +30,11 @@
 - **Login credentials**: `admin` / `password`
 - **Self-signed TLS**: Playwright must be configured to ignore HTTPS errors
   (`ignoreHTTPSErrors: true`)
-- **LLM mock service**: `http://localhost:1080` — enqueue responses before
-  sending messages
+- **LLM mock service**: accessible via Rancher proxy at
+  `https://localhost:8005/api/v1/namespaces/cattle-ai-agent-system/services/http:llm-mock:80/proxy`
+  — enqueue responses before sending messages (direct `localhost:1080` access is
+  not available; all requests must go through the Rancher proxy with the
+  `R_SESS` session cookie)
 
 ---
 
@@ -111,14 +114,14 @@ user-sent message and one assistant reply.
 **Mock LLM Setup** (enqueue before sending user message):
 
 ```http
-PUT http://localhost:1080/mockserver/expectation
+POST https://localhost:8005/api/v1/namespaces/cattle-ai-agent-system/services/http:llm-mock:80/proxy/v1/control/push
 Content-Type: application/json
+Cookie: R_SESS=<session-token>
 
 {
-  "httpRequest": { "method": "POST" },
-  "httpResponse": {
-    "statusCode": 200,
-    "body": "data: {\"type\":\"text\",\"content\":\"Hello from the mock agent.\"}\n\ndata: {\"type\":\"done\"}\n\n"
+  "agent": "rancher",
+  "text": {
+    "chunks": ["Hello from the mock agent."]
   }
 }
 ```
@@ -127,8 +130,8 @@ Content-Type: application/json
 1. Navigate to `https://localhost:8005` and log in.
 2. Press `Alt+K` to open the chat panel.
 3. Wait for `[data-testid="rancher-ai-ui-chat-panel-ready"]`.
-4. Enqueue a mock LLM response via HTTP PUT to
-   `http://localhost:1080/mockserver/expectation` (see Mock LLM Setup above).
+4. Enqueue a mock LLM response via HTTP POST to the Rancher proxy push endpoint
+   (see Mock LLM Setup above; include the `R_SESS` cookie from the browser session).
 5. Click `[data-testid="rancher-ai-ui-chat-input-textarea"]` and type
    `Test message for new-chat shortcut`.
 6. Press `Enter` to send the message.
@@ -197,14 +200,14 @@ most recent assistant message into the system clipboard.
 **Mock LLM Setup** (enqueue before sending user message):
 
 ```http
-PUT http://localhost:1080/mockserver/expectation
+POST https://localhost:8005/api/v1/namespaces/cattle-ai-agent-system/services/http:llm-mock:80/proxy/v1/control/push
 Content-Type: application/json
+Cookie: R_SESS=<session-token>
 
 {
-  "httpRequest": { "method": "POST" },
-  "httpResponse": {
-    "statusCode": 200,
-    "body": "data: {\"type\":\"text\",\"content\":\"Clipboard test content from mock agent.\"}\n\ndata: {\"type\":\"done\"}\n\n"
+  "agent": "rancher",
+  "text": {
+    "chunks": ["Clipboard test content from mock agent."]
   }
 }
 ```
@@ -213,8 +216,8 @@ Content-Type: application/json
 1. Navigate to `https://localhost:8005` and log in.
 2. Press `Alt+K` to open the chat panel.
 3. Wait for `[data-testid="rancher-ai-ui-chat-panel-ready"]`.
-4. Enqueue the mock LLM response via HTTP PUT to
-   `http://localhost:1080/mockserver/expectation`.
+4. Enqueue the mock LLM response via HTTP POST to the Rancher proxy push endpoint
+   (see Mock LLM Setup above; include the `R_SESS` cookie from the browser session).
 5. Click `[data-testid="rancher-ai-ui-chat-input-textarea"]` and type
    `Please give me something to copy`.
 6. Press `Enter` to send the message.
@@ -250,14 +253,14 @@ sent.
 **Mock LLM Setup** (enqueue before sending user message):
 
 ```http
-PUT http://localhost:1080/mockserver/expectation
+POST https://localhost:8005/api/v1/namespaces/cattle-ai-agent-system/services/http:llm-mock:80/proxy/v1/control/push
 Content-Type: application/json
+Cookie: R_SESS=<session-token>
 
 {
-  "httpRequest": { "method": "POST" },
-  "httpResponse": {
-    "statusCode": 200,
-    "body": "data: {\"type\":\"text\",\"content\":\"Delete test response.\"}\n\ndata: {\"type\":\"done\"}\n\n"
+  "agent": "rancher",
+  "text": {
+    "chunks": ["Delete test response."]
   }
 }
 ```
@@ -266,7 +269,8 @@ Content-Type: application/json
 1. Navigate to `https://localhost:8005` and log in.
 2. Press `Alt+K` to open the chat panel.
 3. Wait for `[data-testid="rancher-ai-ui-chat-panel-ready"]`.
-4. Enqueue a mock LLM response.
+4. Enqueue a mock LLM response via HTTP POST to the Rancher proxy push endpoint
+   (see Mock LLM Setup above; include the `R_SESS` cookie from the browser session).
 5. Click `[data-testid="rancher-ai-ui-chat-input-textarea"]` and type
    `Message before deletion`.
 6. Press `Enter` to send.
@@ -274,7 +278,7 @@ Content-Type: application/json
 8. Press `Ctrl+Shift+Backspace`.
 9. Wait up to 3 seconds for a deletion confirmation modal to appear.
 10. Click the confirmation button
-    `[data-testid="rancher-ai-ui-delete-chat-confirm-button"]`.
+    `[data-testid="prompt-remove-confirm-button"]`.
 11. Wait up to 5 seconds for the chat to reset.
 
 **Assertions**:
@@ -287,7 +291,7 @@ Content-Type: application/json
   messages).
 
 **Selectors**:
-- `[data-testid="rancher-ai-ui-delete-chat-confirm-button"]`
+- `[data-testid="prompt-remove-confirm-button"]`
 - `[data-testid="rancher-ai-ui-chat-panel-ready"]`
 - `[data-testid="rancher-ai-ui-chat-input-textarea"]`
 
@@ -370,32 +374,32 @@ the chat panel menu navigates the user to the AI Assistant settings page.
 ## Mock Data Setup
 
 For tests that send messages (Tests 3, 5, 6), enqueue mock LLM responses
-**before** sending the user message. Use an HTTP PUT request:
+**before** sending the user message. Use an HTTP POST request through the
+Rancher proxy (direct `localhost:1080` access is not available):
 
 ```
-PUT http://localhost:1080/mockserver/expectation
+POST https://localhost:8005/api/v1/namespaces/cattle-ai-agent-system/services/http:llm-mock:80/proxy/v1/control/push
 Content-Type: application/json
+Cookie: R_SESS=<session-token>
 
 {
-  "httpRequest": {
-    "method": "POST"
-  },
-  "httpResponse": {
-    "statusCode": 200,
-    "headers": {
-      "Content-Type": ["text/event-stream"]
-    },
-    "body": "data: {\"type\":\"text\",\"content\":\"<YOUR RESPONSE TEXT>\"}\n\ndata: {\"type\":\"done\"}\n\n"
+  "agent": "rancher",
+  "text": {
+    "chunks": ["<YOUR RESPONSE TEXT>"]
   }
 }
 ```
 
 Replace `<YOUR RESPONSE TEXT>` with the response text expected in that test.
+Obtain the `R_SESS` cookie value from the browser session after login (use
+`page.context().cookies()` in Playwright to retrieve it).
 
 To clear enqueued responses after all tests complete:
 
 ```
-PUT http://localhost:1080/mockserver/reset
+POST https://localhost:8005/api/v1/namespaces/cattle-ai-agent-system/services/http:llm-mock:80/proxy/v1/control/clear
+Content-Type: application/json
+Cookie: R_SESS=<session-token>
 ```
 
 ---
@@ -411,7 +415,7 @@ PUT http://localhost:1080/mockserver/reset
 | `[data-testid="rancher-ai-ui-chat-history-panel"]` | `panels/History.vue` | History panel |
 | `[data-testid="rancher-ai-ui-chat-history-panel-overlay"]` | `panels/History.vue` | History panel backdrop |
 | `[data-testid="rancher-ai-ui-chat-input-textarea"]` | `panels/Console.vue` | Message input |
-| `[data-testid="rancher-ai-ui-delete-chat-confirm-button"]` | Dialog | Delete confirmation button |
+| `[data-testid="prompt-remove-confirm-button"]` | `dialog/DeleteChatCard.vue` | Delete confirmation button |
 | `.chat-console-menu-container .icon-actions` | `header/ChatPanelMenu.vue` | Three-dot actions menu trigger |
 | `.shortcuts` | `header/KeyboardShortcuts.vue` | Shortcuts popup container |
 | `.shortcuts-title` | `header/KeyboardShortcuts.vue` | Popup section title |
